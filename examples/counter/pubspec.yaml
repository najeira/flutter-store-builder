name: counter
description: Flux store builder example

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  store_builder:
    path: ../../../flutter-flux

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

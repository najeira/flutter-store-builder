name: counter
description: store builder example

version: 0.4.0+00400000

dependencies:
  flutter:
    sdk: flutter
  store_builder:
    path: ../../../flutter-store-builder

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

environment:
  sdk: '>=2.6.0 <3.0.0'
  flutter: ^1.2.0
